// Package fs implements the object Store interface using the local filesystem.
//
// This is a stub — the full implementation lands in a downstream change.
package fs

import "fmt"

func init() {
	fmt.Println("fs storage stub registered")
}
