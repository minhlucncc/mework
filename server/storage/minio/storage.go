// Package minio implements the object Store interface using MinIO.
//
// This is a stub — the full implementation lands in a downstream change.
package minio

import "fmt"

func init() {
	fmt.Println("minio storage stub registered")
}
